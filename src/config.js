const config = {
    db: {
      /* This database was only for test, there is no implementation about this on this repository */
      host: "localhost",
      user: "root",
      port:'3308',
      password: "0214"
    },
  };
  module.exports = config;